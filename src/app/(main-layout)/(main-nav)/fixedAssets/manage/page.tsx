import ManageContainer from "@/components/fixedAssets/ManageContainer";
import React from "react";

export default function ManageFixedAssets() : React.ReactElement {
    return (
        <ManageContainer/>
    );
}